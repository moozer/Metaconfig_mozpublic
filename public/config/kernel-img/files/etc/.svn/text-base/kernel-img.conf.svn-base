%header("##")
warn_initrd = no
do_symlinks = no
do_bootloader = no
do_bootfloppy = no
do_initrd   = yes
#if not (PLATFORM_OS_NAME == "debian" and PLATFORM_OS_VERSION > "6.0")
  postrm_hook = update-grub
  postinst_hook = update-grub
#end
