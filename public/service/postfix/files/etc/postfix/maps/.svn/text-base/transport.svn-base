%header("##")
#if autoreply_support
  autoreply		autoreply:
#end
#foreach d in mailman_domains
  %(d)	mailman:
#end
#foreach l in transport_lines
  %(l.split("|")[0])    %(l.split("|")[1])
#end
## Default transport everything else
* :
