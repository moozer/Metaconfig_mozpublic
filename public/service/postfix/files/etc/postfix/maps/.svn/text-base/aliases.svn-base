%header("##")
root:           %(", ".join(root_destination))
#foreach alias in root_aliases
  %(alias): root
#end
#foreach alias in aliases
  %(alias.split("|", 1)[0]): %(alias.split("|", 1)[1])
#end
