##
## Configuration for public/service/heartbeat
##

[apt]
install += "heartbeat"

[trigger.reload-heartbeat]
files += "/etc/ha.d/*"
command = ["invoke-rc.d", "heartbeat", "reload"]

[settings]
bcast = ["eth0", "eth1"]
#authkey =
#ping_group =
#nodes =
resource_list = []
#resource_host =

[files]
permission["/etc/ha.d/authkeys"] = "600"
