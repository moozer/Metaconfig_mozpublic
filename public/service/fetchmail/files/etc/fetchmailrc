%header("##")
set daemon %(daemon_interval)
set no bouncemail
defaults:
  antispam -1
  batchlimit 100

#foreach fp in ssl_fingerprints
  sslfingerprint "%(fp)"
#end

poll %(s0_host) proto %(s0_proto) port %(s0_port) %(s0_opts)
  user "%(s0u0_user)" with pass "%(s0u0_pass)" is "%(s0u0_local)" here ssl no rewrite

