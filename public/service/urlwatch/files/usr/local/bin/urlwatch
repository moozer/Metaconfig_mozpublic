%#!/bin/zsh
%header("##")
set -e
URL="$1"
SUM="$2"
PATTERN="$3"
RCPT="$4"
SUBJECT="[urlwatch] $5"
BODY="$URL changed"

[[ $(curl -s $URL | grep "$PATTERN" | md5sum) == $SUM ]] || echo "$BODY" | mail -s "$SUBJECT" $RCPT
